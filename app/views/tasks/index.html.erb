<h1>タスク一覧</h1>

<table>
  <tr>
    <th>タスク名</th>
    <th>詳細</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.detail %></td>
    <td><%= link_to '編集', edit_task_path(task.id) %></td>
    
  </tr>
<% end %>
</table>